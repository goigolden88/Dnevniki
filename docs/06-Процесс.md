# Процесс

Обновлён: 11.09.2026

Как ведётся работа над проектом. Не про то, что строим, а про то, как.

## Где что живёт

| Что | Где |
|---|---|
| Код приложения | публичный репозиторий на GitHub |
| Документы проекта | `docs/` в том же репозитории |
| Инструкции для Claude Code | `CLAUDE.md` в корне репозитория |
| Данные пользователя | отдельный приватный репозиторий |

Документы лежат вместе с кодом, а не в проекте Claude.ai. Причины две: `CLAUDE.md` подхватывается автоматически в каждой сессии, а документы получают версионирование заодно с кодом. Держать их в двух местах нельзя — разъедутся за месяц.

Отсюда правило для бесед в чате: копии в проект Claude.ai кладутся свежие из репозитория и **правятся только в репозитории**. Файл, лежащий в проекте Claude.ai, — снимок, а не документ. Правка, сделанная в снимке, не существует. Надёжнее подключить проект Claude.ai к репозиторию напрямую и убрать ручной перенос совсем.

### Сессии Claude Code

Между этапами — `/clear`, не новая беседа: папка та же, `CLAUDE.md` подхватывается автоматически, сессия начинается со сверки по Журналу и Плану. `/compact` — для середины этапа, когда работа не закончена.

Перед `/clear`: рабочее дерево чистое, всё запушено, и по одному Журналу понятно, где мы и что дальше. Если непонятно — чинится Журнал, а не тащится контекст.

## Разделение работы

**Claude Code — «как это построить».** Код, сборка, деплой, отладка. Плюс любые обсуждения, где нужно видеть настоящие файлы: стоит ли выносить в отдельный модуль, почему тормозит синхронизация, что сломается от этой правки. У него есть режим планирования — можно обсуждать, пока он ничего не трогает.

**Чат — «что вообще строить».** Состав фич, продуктовые развилки, крупные пересмотры архитектуры, приоритеты.

Причина разделения практическая: контекст сессии Claude Code забивается содержимым файлов, и длинное продуктовое обсуждение съедает то, что нужно было на код. Плюс лимиты использования.

## Поток изменений: чат → документы → код

1. Обсуждаем крупное изменение в чате
2. На выходе получаем **готовые куски markdown для вставки в документы**, а не пересказ беседы. Обычно три:
   - запись в `05-Решения` — развилка, выбор, обоснование
   - правка `01-Проект`, если поменялся состав фич
   - правка `02-Архитектура`, если поменялась модель данных
3. Отдельным пунктом — **что придётся переделать в уже написанном коде**. Без этого Claude Code увидит новую спецификацию и противоречащий ей старый код и будет гадать, что из этого правда
4. Вставляем, коммитим
5. Следующая сессия Claude Code начинается со сверки

Смысл в том, что источник истины — документы в репозитории, а не чья-либо память о беседе.

## Обратный поток: код → чат

Claude Code по ходу работы натыкается на дыры в архитектуре. Если в конце сессии он говорит «вот это в документах не описано» — это возвращается в чат для обсуждения, а не решается на месте. Иначе решения принимаются молча и нигде не фиксируются.

## Правила ведения документов

**Не менять задним числом.** Если решение отменяется, старая запись в `05-Решения` остаётся, новая пишется под новым номером со ссылкой «отменяет Р-07». Иначе через полгода будешь смотреть на код и не понимать, почему он сделан не так, как написано — а причина будет в стёртой записи.

**Решения записываются в момент выбора**, не в конце сессии по памяти.

**Журнал обновляется в конце каждой сессии.** Что сделано, в каком состоянии, следующий шаг.

**Перед пушем — `npm run test` и `npm run smoke`.** Первое проверяет расчёт, второе — что приложение открывается и не падает на обычном пути (Р-41). Тестов экранов в проекте нет, и без прогона React-часть не проверяется ничем, кроме глаз на телефоне через неделю.

**План содержит только предстоящее.** Сделанное переезжает в Журнал.

## Шаблоны

Начало сессии по плану:

```
Работаем по docs/03-План.md, следующий этап оттуда.
Прочитай CLAUDE.md и docs/ перед началом.
Сначала покажи план работ, код пиши после подтверждения.
```

Начало сессии после обновления документов в чате:

```
Я обновил docs/. Прочитай их и сверься с кодом.
Скажи, что теперь расходится и в каком порядке это править.
```

Конец сессии:

```
Дай текст для docs/04-Журнал.md: что сделано, в каком состоянии,
следующий шаг. И для docs/05-Решения.md, если по ходу что-то решили.
Отдельно — что осталось не описанным в документах.
```

Начало беседы в чате: приложить документы или дать доступ к репозиторию, дальше обсуждать по существу.

### Передача в следующую сессию

Заготовки выше не зависят от того, где мы стоим, — они про тип сессии. Эта
зависит: она передаёт состояние на конец последнего этапа и переписывается
здесь же в конце каждого следующего. Сейчас передаёт состояние после
сессии 11.09.2026: Этап 6 сделан по коду, хвосты проверяются временем.

```
Проект «Дневники». Работать в корне репозитория `Dnevniki/`, а не в родительской
папке: история бесед привязана к рабочей директории.

Прочитай `CLAUDE.md` и все файлы `docs/` перед началом.

Этапы 0–5 закрыты, Этап 6 сделан по коду и запушен 11.09.2026. Приложение
живёт на https://goigolden88.github.io/Dnevniki/, установлено на телефоне
(Android) и компьютере. 580 тестов и `npm run smoke` — 69 проверок
в браузере через headless Chrome. Версия схемы 2: одна миграция — хранилище
`categories` (Р-59).

Этап 6 добавил: тихие часы и журнал пробуждений напоминаний (Р-57), «Ещё
смотришь?» (Р-58), категории своими записями и переименование всех названий
(Р-59), импорт записей с промптом для ИИ (Р-60), «Настройки» разделами,
«Поделиться» и разделы markdown (Р-61), паузу и повтор при гонке
синхронизации (Р-62), справку (Р-63). Всё, кроме импорта на живом ИИ
и «Поделиться» после исправления, проверено на телефоне.

Код-пунктов в Этапе 6 не осталось, кроме шаблонов тренировок — они ждут
тренировок. Хвосты проверяются временем и лежат в Плане. Следующий этап
не выбран: кандидаты — напоминание в точное время (GitHub Actions или
Google Календарь) и приложение для друга на iPhone. Начни с вопроса, что
решено в чате и что заметилось за эти дни. Пункт попадает в этап вместе
с вопросом, на который он отвечает.

Ключевое, что нужно знать до кода:
— модуль добавляется по чеклисту «Как добавить модуль» в `02-Архитектура.md`.
  Новое хранилище заводит только его миграция: `V1_STORES` в `db.ts`
  не трогается никогда (Р-59);
— реестр — таблица по `EventKind`: лента, markdown и раздел импорта (Р-60).
  Маршруты, вкладки и «Сейчас» через него не идут. Все модули разом знают
  только `app.tsx`, `registry.ts`, `notify.ts` и `screens/`;
— раскладка модуля: расчёт в `modules/<имя>/<имя>.ts` без React и без `db`,
  тексты в `labels.ts`, лента и markdown в `feed.ts`, импорт в `import.ts`,
  база в `use<Имя>.ts`. Тестами покрывается всё, кроме React;
— новые блоки сворачиваются через `ui/Fold`, где уместно — правило
  в `CLAUDE.md`;
— тестов React-экранов нет и не будет (Р-41). Вместо них логика вне
  компонентов и `npm run smoke`. Гоняй его перед пушем и дописывай сценарий:
  он в `scripts/smoke.mjs` одной функцией `scenario`. Итог тестов проверяй
  по коду выхода, а не `grep`'ом по выводу;
— service worker свой, `src/sw.ts`: автообновление держится на `skipWaiting`
  и `clientsClaim` в нём самом, имя на выходе — `sw.js`, менять нельзя;
— слияние по записи, побеждает поздний `updatedAt` (Р-07);
— `settings` не синхронизируется никогда, там токен.

Как работаем: сначала план работ, код после подтверждения. Решения записываем
в `docs/05-Решения.md` в момент выбора, а не в конце по памяти. Отступления от
Архитектуры и от прежних решений проговариваем вслух до того, как писать код.
В конце сессии — Журнал: что сделано, в каком состоянии, следующий шаг, и
отдельно что осталось не описанным в документах. Перед `/clear` дерево чистое
и всё запушено.

Отдельно посмотри блок «Не описано в документах» в верхней записи Журнала,
«Открытые вопросы» в конце `05-Решения.md` и хвосты этапов 3–6 в Плане.
Они не про код, но висят: курс из Obsidian — импортом, заодно первая
проверка промпта; журнал пробуждений напоминаний; «Поделиться» файлом
`.txt`; две позиции «Барьер Эксперт». Справка (`screens/Help.tsx`) ничем
не проверяется: меняешь поведение — правь и её.
```
