# План

Обновлён: 11.09.2026

Здесь только то, что предстоит. Что уже сделано — в Журнале.

## Логика порядка

Циклы идут первыми: они дают пользу с первого дня, не требуя накопления истории — вводишь последние известные даты и сразу получаешь экран просроченного.

Синхронизация идёт раньше остальных модулей, хотя выглядит скучнее. Причина: она затрагивает каждую сущность, и прикручивать её к трём готовым модулям дороже, чем к одному.

Здоровье и контент раскрываются только со временем — первому нужен год данных, второму конец года. Поэтому они позже, но заводить их надо не откладывая, чтобы данные начали копиться.

---

## Этап 3 — Здоровье

Закрыт 10.09.2026: проверено на устройстве, печать сводки работает.
Осталось не по коду, а по данным:

- Перенести здоровье из Obsidian, если там осталось не всё: в базе лежат
  4 эпизода и 2 измерения из переноса
- Решить два вопроса про данные: «заложенность носа» и «насморк» разными
  тегами; две позиции «Барьер Эксперт» в циклах. С Р-59 оба решаются
  в «Настройках» → «Категории и названия»: теги сливаются переименованием,
  позиции собираются группой
- Синхронизация записей здоровья между устройствами не проверялась:
  механика та же, что у циклов, но `health/*.json` ещё не ездили

## Этап 4 — Контент

Закрыт 10.09.2026: проверено на устройстве, итоги 2026 года читаются.
Осталось не по коду, а по данным:

- Проверить, что у перенесённых записей верно проставлен статус: он
  выставлялся правилом «есть оценка → просмотрено», глазами не сверялся
- Курсы: дневник в Obsidian был, в нём один курс. В перенос не попал —
  внести импортом записей (Р-60): заодно первая проверка промпта на живом ИИ
- Синхронизация записей контента между устройствами не проверялась.
  Механика та же, что у циклов, но `content/*.json` ещё не ездили

## Этап 5 — Доводка

Закрыт 11.09.2026: проверено на устройстве, автообновление через свой
service worker сработало. Осталось не по коду:

- Фоновое напоминание ни разу не пришло само — проверялось только кнопкой
  «Проверить сейчас». Приходит примерно раз в сутки по выбору браузера;
  если за несколько дней не пришло ни разу — разбираться
- Напоминание о незакрытой болезни на живом эпизоде
- Синхронизация `templates.json` между устройствами

## Этап 6 — Доведение до ума

Место для того, что всплывает при ежедневном пользовании. Пункт попадает
сюда вместе с вопросом, на который он отвечает, — иначе это пожелание,
а не задача.

Целиком этап не закрывается: пункт закрыт, когда проверен на телефоне.

Сделано 11.09.2026 — всё записанное и всплывшее по ходу, подробности
в Журнале: Р-57…Р-63, заметка у тренировки, свои метрики, подгруппы
в «Настройках». Узкий экран проверен на телефоне. Осталось:

- **Импорт на живом ИИ** (Р-60). Проверен тестами и прогоном, на настоящих
  заметках — нет. Первым — курс из Obsidian
- **Фоновое напоминание** (Р-57). Приходит ли само — видно в журнале
  пробуждений в «Настройках». Если за несколько дней журнал пуст, браузер
  проверку не будит — разбираться
- **«Ещё смотришь?» на живых записях** (Р-58) — около 7 декабря: пять
  перенесённых «смотрю» считаются от даты переноса 08.09
- **«Поделиться» файлом `.txt`** — проверить на телефоне после исправления
- **Шаблоны тренировок.** «Бег 5 км одним тапом» — когда тренировки начнут
  записываться (Р-49)

## Дальше — выбирается в чате

Следующих этапов в Плане нет. Кандидаты, оба в открытых вопросах
`05-Решения`:

- **Напоминание в точное время и на iPhone.** Без сервера невозможно (Р-50,
  Р-57). Задача по расписанию в GitHub Actions или Google Календарь
- **Приложение для друга на iPhone.** Установка через Safari, синхронизация
  своим токеном или жизнь на копиях файлом, напоминаний без сервера нет

## Отложено

- Дневник Депры — другой тип данных, решение о включении не принято
- Интеграция с приложениями по еде и тайм-менеджменту
- Использование поля `refs` для связей между модулями
- Напоминания на iPhone — возможны только со своим сервером (Р-50)
- Автомобильные траты и заправки — в приложение про деньги, не сюда (Р-35)
- Интервал по пробегу для авто — второй вид интервала у позиции цикла
- Расшифровка и классификация дневника «Исходров»
- Подтяжка обложек и метаданных контента из внешних баз. По Р-12 она
  необязательная кнопка, а стоит дорого: выбрать сервис, решить вопрос
  с CORS без бэкенда и с ключами, которым в публичном репозитории не место.
  Ни на один вопрос этапа — итоги года, оценки, что смотрю — обложка
  не отвечает
