import { Link } from 'react-router-dom'
import { plural } from '../core/dates.ts'
import { MARKS_FOR_INTERVAL } from '../modules/cycles/labels.ts'
import { InstallNote } from '../ui/Install.tsx'

/**
 * Приветствие на «Сейчас» при первом запуске (Р-70): что это, с чего начать
 * в каждом модуле, как поставить иконкой, где справка.
 *
 * Показывается, пока в базе нет ни одной записи и его не закрыли. Первая
 * запись убирает его сама — дело сделано; «Понятно» — насовсем. Всё, что
 * здесь сказано, есть и в справке, так что закрыть его не страшно.
 *
 * Числа — из констант, цифрой в этом файле не пишется ничего: это проверяет
 * тест, как у справки (Р-65, Р-67).
 */
export function Welcome({ onDone }: { onDone: () => void }) {
  return (
    <section className="panel block">
      <h2>С чего начать</h2>
      <p>
        Дневник того, что повторяется, болезней и того, что смотришь. Всё хранится на этом устройстве
        и работает без сети; без настройки наружу ничего не уходит.
      </p>
      <ul>
        <li>
          <b>Циклы</b> — здесь, ниже: «Добавить позицию» — стрижка, фильтр, ТО, что угодно
          повторяющееся. «Отметить» ставит сегодняшний день. Срок посчитается сам после{' '}
          {MARKS_FOR_INTERVAL} {plural(MARKS_FOR_INTERVAL, ['отметки', 'отметок', 'отметок'])} — или задай
          его руками. Категории свои: «Настройки» → «Категории и названия».
        </li>
        <li>
          <b>
            <Link to="/health">Здоровье</Link>
          </b>{' '}
          — заболел, «Завести эпизод»; там же вес, давление и тренировки.
        </li>
        <li>
          <b>
            <Link to="/content">Контент</Link>
          </b>{' '}
          — «Добавить запись»: что смотришь, читаешь, во что играешь, чему учишься.
        </li>
      </ul>
      <p>
        Уже ведёшь на другом устройстве — <Link to="/settings">«Настройки»</Link> → «Синхронизация»
        или «Экспорт и импорт» → «Восстановить из копии». Записи из заметок и других сервисов
        переносятся там же, «Импорт записей», с помощью ИИ.
      </p>

      <h3 className="unit__name">Установка</h3>
      <InstallNote empty />

      <div className="row row--wrap">
        <button type="button" className="btn btn--primary" onClick={onDone}>
          Понятно
        </button>
        <Link className="btn" to="/help">
          Справка
        </Link>
      </div>
    </section>
  )
}
